<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Quran" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground">@color/Green</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/icon_sc_two</item>
        <item name="colorPrimary">@color/Green</item>
        <item name="colorSurface">@color/Green</item>
    </style>


    <style name="Theme.Quran" parent="Base.Theme.Quran" />
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/rounded_corner</item>
    </style>
</resources>